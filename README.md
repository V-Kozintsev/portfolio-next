src/
├── app/           # маршруты, layout, глобальные стили
├── widgets/       # крупные блоки (Header, Footer, SkillsSection и т.д.)
├── features/      # сценарии (например, переключение темы)
├── entities/      # бизнес-сущности (Project, Skill, Experience)
└── shared/        # переиспользуемые UI-компоненты, утилиты, API
    ├── ui/        # простые компоненты (Button, Card)
    ├── lib/       # вспомогательные функции
    └── api/       # клиенты API, запросы